# Structure Engine

Use first. Structure second.

A structure discovery engine that observes usage patterns and automatically proposes nodes, relationships, categories, clusters, and hierarchies from real behavior.

## Core Inversion

Most systems force structure before use.

```txt
Human
  ↓
Creates folders
  ↓
Creates categories
  ↓
Creates hierarchy
  ↓
Uses system
